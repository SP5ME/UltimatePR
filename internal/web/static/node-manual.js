const nodeManualText={
  pl:{title:'NODE — podręcznik',intro:'Praktyczny przewodnik konfiguracji noda packet-radio w UltimatePR. Opisy odpowiadają aktualnemu działaniu programu.',toc:'Spis treści',sections:[
    ['what','1. Czym jest NODE','<p>NODE jest węzłem sieci packet-radio. Użytkownik łączy się z nodem, a następnie może przejść dalej do innego noda, BBS lub innej usługi. NODE odpowiada przede wszystkim za połączenia i routing.</p><pre>Stacja\n  |\n  v\nNODE A\n  |\n  +---- BBS\n  |\n  +---- NODE B\n          |\n          +---- NODE C</pre>'],
    ['roles','2. NODE, BBS i digipeater','<ul><li><strong>NODE</strong> zestawia dalsze połączenia i zna sąsiadów oraz trasy.</li><li><strong>BBS</strong> przechowuje wiadomości i jest usługą końcową.</li><li><strong>Digipeater</strong> retransmituje ramki AX.25. Nie jest tym samym co node routujący.</li></ul>'],
    ['connect','3. Jak działa CONNECT','<p>W powłoce NODE komenda <code>C KRK01</code> powoduje wyszukanie trasy do celu. Program wybiera najlepszą znaną trasę według Quality i próbuje połączyć się przez wskazanego sąsiada.</p><div class="manual-note"><strong>Uwaga:</strong> sortowanie kilku tras działa. Pełny automatyczny retry/failover po nieudanym połączeniu nie jest osobną, w pełni zaimplementowaną funkcją.</div>'],
    ['identity','4. Callsign i Alias','<pre>Callsign: SR5XYZ-5\nAlias: WAW01</pre><p>Callsign to właściwy znak AX.25 noda. Alias to krótka nazwa używana w sieci i w znaku zachęty powłoki.</p>'],
    ['interfaces','5. Interfejsy','<p>NODE korzysta z istniejących interfejsów UltimatePR, a nie z osobnego systemu portów. Przykładowe interfejsy:</p><pre>VHF-1200\nUHF-9600</pre><p>Interfejs określa, przez który kanał lub TNC node komunikuje się z sąsiadem. Numer KISS nie jest tu głównym poziomem konfiguracji.</p>'],
    ['neighbors','6. Bezpośredni sąsiedzi','<p>Bezpośredni sąsiad to inny node, z którym UltimatePR może połączyć się bezpośrednio przez wybrany interfejs.</p><pre>SR5XYZ-5\nInterfejs: VHF-1200\nPreferencja: 200</pre><p>W panelu wybierz aktywny interfejs z listy. Callsign identyfikuje sąsiada, a preferencja wpływa na wybór drogi.</p>'],
    ['known','7. Znane nody','<p>W sieci <code>A -&gt; B -&gt; C</code> dla noda A node B jest bezpośrednim sąsiadem, natomiast B i C są znanymi nodami. Znany node nie musi być osiągalny jednym skokiem.</p><p>Komenda <code>NODES</code> pokazuje skonfigurowanych bezpośrednich sąsiadów. Komenda <code>ROUTES</code> pokazuje znane cele i drogę przez sąsiada.</p>'],
    ['manual-routes','8. Trasy ręczne','<p>Trasa ręczna mówi node\'owi, przez którego bezpośredniego sąsiada ma próbować dotrzeć do określonego celu.</p><pre>Cel: KRK01\nPrzez: WAW01\nPreferencja: 180</pre><p>Aby dotrzeć do KRK01, node użyje sąsiada WAW01. Trasy ręczne nie są usuwane przez mechanizm wygasania tras automatycznych.</p>'],
    ['learned','9. Trasy automatyczne','<p>Trasa automatyczna jest nauczona z informacji routingowych wysyłanych przez inne nody. Nie wpisuje się jej ręcznie. Runtime zapisuje sąsiada, cel, Quality, czas aktualizacji i licznik wygasania.</p>'],
    ['quality','10. Preferencja trasy (Quality)','<p>Quality określa, którą z kilku tras node woli użyć. Wyższa wartość oznacza bardziej preferowaną drogę.</p><pre>WAW01  220\nWAW02  180\n\nNode preferuje WAW01.</pre><p><strong>Zakres: 0–255.</strong> Quality nie jest RSSI ani siłą sygnału.</p>'],
    ['expiry','11. Wygasanie tras (Obsolescence)','<p>Node zapamiętuje trasy nauczone automatycznie. Jeśli przez dłuższy czas nie dostaje ponownej informacji, ich ważność maleje. Po wygaśnięciu trasa jest usuwana.</p><pre>Node nauczył się trasy do KRK01.\nKolejne informacje przestają przychodzić.\nPo osiągnięciu końca licznika trasa znika.</pre><p>Wartość jest licznikiem cykli starzenia. Dotyczy tras dynamicznych, nie ręcznych.</p>'],
    ['broadcasts','12. Informacje o trasach NET/ROM','<p>Node okresowo informuje sąsiadów, jakie nody i trasy zna. Ten routing broadcast jest wysyłany tylko po włączeniu NET/ROM.</p><pre>Interwał: 600 s\nNode wysyła informacje co 10 minut.</pre>'],
    ['minimum','13. Minimalna jakość trasy','<p>Node ignoruje trasy o wartości niższej od ustawionego minimum.</p><pre>Minimum: 100\nTrasa 180 -> przyjęta\nTrasa 80  -> odrzucona</pre><p>Jest to filtr Quality, a nie pomiar poziomu sygnału.</p>'],
    ['maximum','14. Maksymalna liczba zapamiętanych nodów','<p>Ogranicza liczbę celów umieszczanych w generowanych informacjach routingowych NET/ROM. Zakres ustawienia to 1–255.</p>'],
    ['example','15. Przykład prostego noda','<pre>Callsign: SR5XYZ-5\nAlias: WAW01\n\nInterface: VHF-1200\n\nNeighbor: SR5ABC-5\nInterface: VHF-1200\nQuality: 200</pre><ol><li>Użytkownik łączy się z SR5XYZ-5.</li><li>Node działa pod aliasem WAW01.</li><li>SR5ABC-5 jest bezpośrednim sąsiadem.</li><li>Połączenia do sąsiada wychodzą przez VHF-1200.</li></ol>'],
    ['multi','16. Przykład dwóch interfejsów','<pre>WAW02 -> VHF-1200\nWAW03 -> UHF-9600</pre><p>Node może odebrać połączenie przez jeden interfejs i zestawić dalszą sesję przez drugi, jeśli oba interfejsy i sąsiednie trasy są aktywne.</p>'],
    ['fallback','17. Przykład trasy zapasowej','<pre>KRK01 via WAW02 Quality 220\nKRK01 via WAW03 Quality 180</pre><p>WAW02 jest preferowany. Kolejna trasa może być użyta tylko w zakresie obsługiwanym przez aktualny runtime; pełny aktywny failover nie jest obecnie gwarantowany.</p>'],
    ['problems','18. Najczęstsze problemy','<h4>Node nie widzi sąsiada</h4><p>Sprawdź, czy interface działa, callsign jest poprawny, neighbor jest aktywny i połączenie AX.25 jest możliwe.</p><h4>Trasa nie jest wybierana</h4><p>Sprawdź Quality, Minimalną jakość trasy, sąsiada, interface i dostępność celu.</p><h4>Trasa automatyczna zniknęła</h4><p>Prawdopodobnie wygasła, ponieważ node przestał otrzymywać jej aktualizację.</p>'],
    ['diagnostics','19. Diagnostyka','<p>W powłoce NODE dostępne są:</p><table><thead><tr><th>Komenda</th><th>Co pokazuje</th></tr></thead><tbody><tr><td><code>HELP</code></td><td>Listę komend powłoki.</td></tr><tr><td><code>NODES</code></td><td>Bezpośrednich sąsiadów noda.</td></tr><tr><td><code>ROUTES</code></td><td>Znane cele i drogę przez sąsiada.</td></tr><tr><td><code>PORTS</code></td><td>Skonfigurowane logiczne interfejsy.</td></tr><tr><td><code>SERVICES</code></td><td>Usługi widoczne z powłoki NODE.</td></tr><tr><td><code>BYE</code></td><td>Kończy sesję.</td></tr></tbody></table>'],
    ['glossary','20. Słownik pojęć','<dl><dt>Callsign</dt><dd>Właściwy znak AX.25 stacji lub noda.</dd><dt>Alias</dt><dd>Krótka nazwa noda.</dd><dt>Neighbor</dt><dd>Node osiągalny bezpośrednio.</dd><dt>Node / Destination</dt><dd>Node lub cel znany z routingu.</dd><dt>Route</dt><dd>Droga do określonego celu.</dd><dt>Quality</dt><dd>Preferencja trasy w zakresie 0–255, nie RSSI.</dd><dt>Obsolescence</dt><dd>Mechanizm wygaszania nieaktualnych tras automatycznych.</dd><dt>Broadcast</dt><dd>Okresowe rozgłaszanie informacji routingowych.</dd><dt>Interface</dt><dd>Kanał komunikacyjny używany przez UltimatePR.</dd></dl>']
  ]},
  en:{title:'NODE — manual',intro:'A practical guide to configuring an UltimatePR packet-radio node. The descriptions match the current program behavior.',toc:'Contents',sections:[
    ['what','1. What is a NODE','<p>A NODE is a packet-radio network node. A user connects to the node and can then continue to another node, BBS, or service. The NODE mainly handles connections and routing.</p><pre>Station\n  |\n  v\nNODE A\n  |\n  +---- BBS\n  |\n  +---- NODE B\n          |\n          +---- NODE C</pre>'],
    ['roles','2. NODE, BBS, and digipeater','<ul><li><strong>NODE</strong> establishes onward connections and knows neighbors and routes.</li><li><strong>BBS</strong> stores messages and is an end service.</li><li><strong>Digipeater</strong> retransmits AX.25 frames. It is not the same as a routing node.</li></ul>'],
    ['connect','3. How CONNECT works','<p>In the NODE shell, <code>C KRK01</code> searches for a route to the destination. The program selects the best known route by Quality and tries to connect through the selected neighbor.</p><div class="manual-note"><strong>Note:</strong> route sorting works. A complete automatic retry/failover after a failed connection is not implemented as a separate guaranteed feature.</div>'],
    ['identity','4. Callsign and Alias','<pre>Callsign: SR5XYZ-5\nAlias: WAW01</pre><p>Callsign is the node AX.25 address. Alias is a short name used in the network and in the shell prompt.</p>'],
    ['interfaces','5. Interfaces','<p>The NODE uses existing UltimatePR interfaces, not a separate port system.</p><pre>VHF-1200\nUHF-9600</pre><p>An interface identifies the channel or TNC used to communicate with a neighbor. The KISS number is not the primary configuration level here.</p>'],
    ['neighbors','6. Direct neighbors','<p>A direct neighbor is another node that UltimatePR can reach directly through the selected interface.</p><pre>SR5XYZ-5\nInterface: VHF-1200\nPreference: 200</pre><p>Select an active interface from the list. Callsign identifies the neighbor; preference affects route selection.</p>'],
    ['known','7. Known nodes','<p>In the network <code>A -&gt; B -&gt; C</code>, B is a direct neighbor of A, while B and C are known nodes. A known node does not have to be one hop away.</p><p><code>NODES</code> lists configured direct neighbors. <code>ROUTES</code> lists known destinations and the neighbor used to reach them.</p>'],
    ['manual-routes','8. Manual routes','<p>A manual route tells the node which direct neighbor to use when trying to reach a destination.</p><pre>Destination: KRK01\nVia: WAW01\nPreference: 180</pre><p>To reach KRK01, the node uses WAW01. Manual routes are not removed by automatic-route expiry.</p>'],
    ['learned','9. Automatic routes','<p>An automatic route is learned from routing information sent by other nodes. It is not entered manually. Runtime stores the neighbor, destination, Quality, update time, and expiry counter.</p>'],
    ['quality','10. Route preference (Quality)','<p>Quality determines which of several routes the node prefers. A higher value means a more preferred route.</p><pre>WAW01  220\nWAW02  180\n\nThe node prefers WAW01.</pre><p><strong>Range: 0–255.</strong> Quality is not RSSI or signal strength.</p>'],
    ['expiry','11. Route expiry (Obsolescence)','<p>The node remembers automatically learned routes. If it stops receiving updates, their validity decreases. After expiry, the route is removed.</p><pre>The node learned a route to KRK01.\nUpdates stop arriving.\nThe route disappears when the counter expires.</pre><p>The value is an ageing-cycle counter. It applies to dynamic, not manual, routes.</p>'],
    ['broadcasts','12. NET/ROM route information','<p>The node periodically tells neighbors which nodes and routes it knows. This routing broadcast is sent only when NET/ROM is enabled.</p><pre>Interval: 600 s\nThe node sends information every 10 minutes.</pre>'],
    ['minimum','13. Minimum route quality','<p>The node ignores routes whose value is below the configured minimum.</p><pre>Minimum: 100\nRoute 180 -> accepted\nRoute 80  -> rejected</pre><p>This is a Quality filter, not a signal-level measurement.</p>'],
    ['maximum','14. Maximum remembered nodes','<p>Limits the number of destinations included in generated NET/ROM route information. The setting range is 1–255.</p>'],
    ['example','15. Simple node example','<pre>Callsign: SR5XYZ-5\nAlias: WAW01\n\nInterface: VHF-1200\n\nNeighbor: SR5ABC-5\nInterface: VHF-1200\nQuality: 200</pre><ol><li>The user connects to SR5XYZ-5.</li><li>The node uses the WAW01 alias.</li><li>SR5ABC-5 is a direct neighbor.</li><li>Connections to that neighbor use VHF-1200.</li></ol>'],
    ['multi','16. Two-interface example','<pre>WAW02 -> VHF-1200\nWAW03 -> UHF-9600</pre><p>The node can receive a connection through one interface and establish the onward session through another, if both interfaces and neighbor routes are active.</p>'],
    ['fallback','17. Backup-route example','<pre>KRK01 via WAW02 Quality 220\nKRK01 via WAW03 Quality 180</pre><p>WAW02 is preferred. The next route can be used only within the behavior supported by the current runtime; full active failover is not guaranteed.</p>'],
    ['problems','18. Common problems','<h4>Node cannot see a neighbor</h4><p>Check that the interface is working, the callsign is correct, the neighbor is enabled, and an AX.25 connection is possible.</p><h4>A route is not selected</h4><p>Check Quality, minimum route quality, neighbor, interface, and destination availability.</p><h4>An automatic route disappeared</h4><p>It probably expired because the node stopped receiving its updates.</p>'],
    ['diagnostics','19. Diagnostics','<p>The NODE shell provides:</p><table><thead><tr><th>Command</th><th>What it shows</th></tr></thead><tbody><tr><td><code>HELP</code></td><td>Available shell commands.</td></tr><tr><td><code>NODES</code></td><td>Direct node neighbors.</td></tr><tr><td><code>ROUTES</code></td><td>Known destinations and their neighbor path.</td></tr><tr><td><code>PORTS</code></td><td>Configured logical interfaces.</td></tr><tr><td><code>SERVICES</code></td><td>Services visible from the NODE shell.</td></tr><tr><td><code>BYE</code></td><td>Ends the session.</td></tr></tbody></table>'],
    ['glossary','20. Glossary','<dl><dt>Callsign</dt><dd>The AX.25 address of a station or node.</dd><dt>Alias</dt><dd>A short node name.</dd><dt>Neighbor</dt><dd>A directly reachable node.</dd><dt>Node / Destination</dt><dd>A node or destination known to routing.</dd><dt>Route</dt><dd>A path to a destination.</dd><dt>Quality</dt><dd>Route preference from 0–255, not RSSI.</dd><dt>Obsolescence</dt><dd>Expiry mechanism for stale automatic routes.</dd><dt>Broadcast</dt><dd>Periodic announcement of routing information.</dd><dt>Interface</dt><dd>A communication channel used by UltimatePR.</dd></dl>']
  ]}
};
function renderNodeManual(lang){const root=document.getElementById('nodeManualContent');if(!root)return;const text=nodeManualText[lang==='en'?'en':'pl'];root.innerHTML=`<h2>${text.title}</h2><p class="manual-intro">${text.intro}</p><h3>${text.toc}</h3><nav class="node-manual-toc">${text.sections.map(s=>`<a href="#node-manual-${s[0]}">${s[1]}</a>`).join('')}</nav>${text.sections.map(s=>`<section id="node-manual-${s[0]}" class="node-manual-section"><h3>${s[1]}</h3>${s[2]}</section>`).join('')}<p class="manual-source">${lang==='en'?'Technical reference: TAPR NET/ROM material and the current UltimatePR runtime.':'Źródło techniczne: materiały TAPR dotyczące NET/ROM oraz aktualny runtime UltimatePR.'}</p>`}
document.getElementById('applicationLanguage')?.addEventListener('change',()=>{if(!document.getElementById('infoNodeManualPanel')?.hidden)renderNodeManual(document.documentElement.lang)});
document.getElementById('openNodeManual')?.addEventListener('click',()=>{if(typeof showPage==='function'){showPage('info');showInfoPart('node')}});
